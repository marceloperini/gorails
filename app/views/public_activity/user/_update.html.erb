<li>
  <i class="si si-plus text-success"></i>
  <%if current_user ==activity.trackable %>
      você atualizou seu usuário
  <%else%>
      <div class="font-w600">atualizou o usuário</div>
      <% if activity.trackable %>
          <div><a href="javascript:void(0)"><%= link_to activity.trackable.event_name, activity.trackable %></a></div>
      <% else %>
          que já foi removido
      <% end %>
  <%end%>

  <div><small class="text-muted"><%=activity.trackable.created_at.strftime("%d/%M/%Y as %H:%M")%></small></div>
</li>